# Staging 真实备份恢复与跨版本回滚演练记录

> 使用说明：复制本文件到同一目录，并按 `YYYY-MM-DD-<environment>.md` 命名。只填写脱敏信息；不得写入密钥、完整数据库连接串、用户内容或未脱敏日志。

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| 演练日期 | `YYYY-MM-DD` |
| 环境标识 | `<staging 名称>` |
| 开始时间 / 结束时间 | `<含时区>` |
| 执行人 | `<姓名/账号>` |
| 观察人 | `<姓名/账号>` |
| 发布负责人 | `<姓名/账号>` |
| 变更单 / 事件单 | `<链接或编号；无则填“不适用”>` |

## 2. 版本与前置条件

| 项目 | 内容 |
| --- | --- |
| 候选 commit/tag | `<candidate>` |
| 候选制品 | `<制品名称、版本、位置>` |
| 候选实际 CI 运行 | `<链接或流水线编号>` |
| 上一稳定 commit/tag | `<stable>` |
| 上一稳定制品 | `<制品名称、版本、位置>` |
| `DATABASE_URL` 脱敏摘要 | `<例如 sqlite://.../staging.db>` |
| 快照根目录 | `<受控存储位置>` |
| 健康检查地址 | `<脱敏后的 health URL>` |

开始操作前逐项确认：

- [ ] staging 写入窗口已冻结，旧 server 与后台写入进程已停止或纳入记录。
- [ ] 候选版本和上一稳定版本的制品均可访问，启动方式已确认。
- [ ] 候选版本的实际 CI 必需检查已通过，并已保存链接或流水线编号。
- [ ] 已执行 `node scripts/staging-db-rollback-drill.mjs status`，并确认数据库路径和现有文件符合预期。
- [ ] 快照空间、操作人和回滚权限已确认。
- [ ] JWT、CORS、API Key 加密密钥等配置来自受保护配置。

## 3. 执行记录

| 步骤 | 时间 | 操作与命令摘要 | 结果 | 证据位置 |
| --- | --- | --- | --- | --- |
| 写入冻结与状态检查 | `<时间>` | `status` 输出摘要 | `<通过/失败>` | `<日志或附件>` |
| 生成演练前快照 | `<时间>` | `snapshot pre-release` | `<通过/失败>` | `snapshotDir: <路径>` |
| 候选版本启动 | `<时间>` | `<实际部署方式>` | `<通过/失败>` | `<启动日志>` |
| 候选 `/health` | `<时间>` | `<请求摘要>` | `<通过/失败>` | `<响应摘要>` |
| 候选业务冒烟 | `<时间>` | bootstrap、已有项目读取 | `<通过/失败>` | `<结果摘要>` |
| 停止候选并恢复快照 | `<时间>` | `restore <snapshotDir>` | `<通过/失败>` | `<restore 输出>` |
| 上一稳定版本启动 | `<时间>` | `<实际部署方式>` | `<通过/失败>` | `<启动日志>` |
| 回滚后业务冒烟 | `<时间>` | bootstrap、已有项目读取 | `<通过/失败>` | `<结果摘要>` |
| 解除冻结与收尾 | `<时间>` | `<恢复的安全版本>` | `<完成/未完成>` | `<通知或记录>` |

## 4. 时间指标

| 指标 | 数值 |
| --- | --- |
| 快照耗时 | `<分钟>` |
| 候选启动至健康检查 | `<分钟>` |
| 候选业务冒烟耗时 | `<分钟>` |
| 快照恢复耗时 | `<分钟>` |
| 上一稳定版本启动至健康检查 | `<分钟>` |
| 总写入冻结窗口 | `<分钟>` |

## 5. 验收证据

- [ ] 主库、WAL、SHM 和 `metadata.json` 均已生成且可访问。
- [ ] 候选版本成功启动，健康检查通过，日志没有新的迁移/补数兼容错误。
- [ ] `/api/workspace/bootstrap` 成功返回。
- [ ] 至少一个已有项目的对话、资产、agent 配置和任务状态可读。
- [ ] 恢复快照完成，并保留 `_pre_restore_<timestamp>` 备份目录信息。
- [ ] 上一稳定版本成功启动，回滚后健康检查和已有项目读取均通过。
- [ ] 无未解释的数据丢失、结构错误或不可读记录。

## 6. 异常与问题

| 时间 | 现象 | 严重级别 | 立即处置 | 问题单/负责人 | 是否影响 Go |
| --- | --- | --- | --- | --- | --- |
| `<时间>` | `<现象>` | `<P0/P1/P2>` | `<处置>` | `<编号/负责人>` | `<是/否>` |

如无异常，填写：`无异常；候选、恢复和回切过程均符合验收标准。`

## 7. 结论

- [ ] **Go：** 全部验收项通过，无 P0/P1 未决问题，证据可追溯，可进入内部试用。
- [ ] **No-Go：** 任一验收项失败、发生回滚失败、存在未解释数据差异或记录不完整；保持稳定版本并重新演练。

结论说明：

`<填写事实、遗留风险和下一步；不要只填写“通过”。>`

## 8. 留痕确认

| 角色 | 姓名 | 确认时间 | 备注 |
| --- | --- | --- | --- |
| 执行人 | `<姓名>` | `<时间>` | `<备注>` |
| 测试/观察人 | `<姓名>` | `<时间>` | `<备注>` |
| 运维 | `<姓名>` | `<时间>` | `<备注>` |
| 发布负责人 | `<姓名>` | `<时间>` | `<备注>` |
