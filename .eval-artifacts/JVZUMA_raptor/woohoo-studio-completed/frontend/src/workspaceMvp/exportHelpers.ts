// MVP backward-compatibility helpers.
// Re-export from utils/exportBundle so legacy workspaceMvp imports keep working.
export {
  exportFullProjectBundle,
  exportCoreProjectBundle,
  createProjectSnapshot,
} from '../utils/exportBundle';
